export interface MediaResponse {
  id: number;
  url: string;
  fileName: string;
  mimeType: string;
  category: string;
  subCategory: string;
  altText: string;
  uploadedBy: number;
  createdAt: string;
}
